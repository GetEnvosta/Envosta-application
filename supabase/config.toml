[api]
enabled = true
port = 54321
schemas = ["public"]

[db]
port = 54322
major_version = 15

[auth]
enabled = true
site_url = "https://envosta-application.vercel.app"
additional_redirect_urls = ["https://envosta-application.vercel.app/auth/callback"]

[auth.email]
enable_signup = true
enable_confirmations = false

# Edge Functions removed entirely — Phase 2 cutover moved all server-side
# work to Vercel API routes (/api/internal/*, /api/admin/*, etc.) using
# Vercel Static IPs. The previous [functions.*] blocks for `log-action`
# and `stripe-checkout` were deleted along with `supabase/functions/`.
