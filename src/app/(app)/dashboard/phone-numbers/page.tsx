export const dynamic = 'force-dynamic';

import { PhoneNumbersDashboard } from '@/components/phone-numbers/phone-numbers-dashboard';

export default function PhoneNumbersPage() {
  return <PhoneNumbersDashboard />;
}
